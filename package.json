{
  "name": "ddc-workspace",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "setup": "bash scripts/setup.sh",
    "build:libs": "npm run build --workspace=packages/config && npm run build --workspace=packages/db && npm run build --workspace=packages/data-pumper",
    "dev": "npm run build:libs && concurrently -n server,webapp -c magenta,green \"npm run dev --workspace=packages/server\" \"npm run dev --workspace=packages/webapp\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "dotenv-mono": "^1.3.14"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
