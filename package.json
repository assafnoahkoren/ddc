{
  "name": "ddc-workspace",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "setup": "bash scripts/setup.sh",
    "dev": "concurrently -n db,server,webapp,pumper -c cyan,magenta,green,yellow \"npm run dev --workspace=packages/db\" \"npm run dev --workspace=packages/server\" \"npm run dev --workspace=packages/webapp\" \"npm run build --workspace=packages/data-pumper\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "dotenv-mono": "^1.3.14"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
