{
  "name": "@ddc/db",
  "version": "1.0.0",
  "description": "Database client for DDC catalog",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate dev",
    "prisma:studio": "prisma studio",
    "prisma:push": "prisma db push",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "prisma",
    "database",
    "catalog"
  ],
  "author": "",
  "license": "ISC",
  "prisma": {
    "schema": "prisma/schema"
  },
  "dependencies": {
    "@prisma/client": "^6.18.0",
    "dotenv-mono": "^1.3.14"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "prisma": "^6.18.0",
    "typescript": "^5.0.0"
  }
}
